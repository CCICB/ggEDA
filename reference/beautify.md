# Make strings prettier for printing

Takes an input string and 'beautify' by converting underscores to spaces
and

## Usage

``` r
beautify(string, autodetect_units = TRUE)
```

## Arguments

- string:

  input string

- autodetect_units:

  automatically detect units (e.g. mm, kg, etc) and wrap in brackets.

## Value

string
